Create a sample CRUD application with User interface
- backend with dotnet core
- frontend with ReactJS + shadcn ui
- server folder has working webapp
- app-ui has template project
- database will be postgreSQL
